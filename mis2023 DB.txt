## Member
CREATE TABLE TBL_MEMBER (
    USERID VARCHAR2(50) NOT NULL,
    USERPW VARCHAR2(50) NOT NULL,
    USERNAME VARCHAR2(50) NOT NULL,
    EMAIL VARCHAR2(100),
    REGDATE DATE DEFAULT SYSDATE,
    UPDATEDATE DATE DEFAULT SYSDATE,
    PRIMARY KEY (USERID)
);

select * from TBL_MEMBER;

SELECT * FROM TBL_MEMBER
				WHERE USERID = ''
                
SELECT * FROM TBL_MEMBER
				WHERE USERID = '' AND USERPW = ''

## USER				
CREATE TABLE TBL_USER (
USID VARCHAR2(50) NOT NULL, 
 UPW VARCHAR2(50) NOT NULL, 
 UNAME VARCHAR2(100) NOT NULL, 
 UPOINT NUMBER DEFAULT 0 NOT NULL, 
 PRIMARY KEY (USID)
 );
 
 select * from TBL_USER;
 
--기초데이터
INSERT INTO TBL_USER(USID,UPW,UNAME) VALUES ('user00','user00','IRON MAN');
INSERT INTO TBL_USER(USID,UPW,UNAME) VALUES ('user01','user01','CAPTAIN');
INSERT INTO TBL_USER(USID,UPW,UNAME) VALUES ('user02','user02','HULK');
INSERT INTO TBL_USER(USID,UPW,UNAME) VALUES ('user03','user03','Thor');
INSERT INTO TBL_USER(USID,UPW,UNAME) VALUES ('user10','user10','Quick Silver');                
commit
				

## Board 
CREATE TABLE TBL_BOARD (
	BNO NUMBER NOT NULL, 
	TITLE VARCHAR2(200) NOT NULL, 
	CONTENT VARCHAR2(1000) NOT NULL, 
	WRITER VARCHAR2(50) NOT NULL, 
	REGDATE DATE DEFAULT SYSDATE, 
	VIEWCNT NUMBER DEFAULT 0, 
	PRIMARY KEY (BNO)
 );
 
CREATE SEQUENCE SEQ_BOARD INCREMENT BY 1 START WITH 1;

select * from TBL_BOARD;

--페이징 테스트를 위해 데이터 INSERT
INSERT INTO TBL_BOARD( bno, title, content, writer, regdate, viewcnt)
SELECT SEQ_BOARD.nextval, title, content, writer, regdate, viewcnt FROM TBL_BOARD;

SELECT * FROM TBL_BOARD WHERE bno > 0 and (rownum >= 1 AND rownum <= 10);

commit;

SELECT COUNT(*) FROM TBL_BOARD;

-- 가상의 veiw를 가져와 뒤집어서 RNUM을 count 하도록
SELECT * FROM( 
			SELECT ROWNUM RNUM, BNO, TITLE, CONTENT, WRITER, REGDATE, VIEWCNT
				FROM (SELECT * FROM TBL_BOARD ORDER BY BNO DESC)
				);
				
SELECT * FROM( 
			SELECT ROWNUM RNUM, BNO, TITLE, CONTENT, WRITER, REGDATE, VIEWCNT
				FROM (SELECT * FROM TBL_BOARD ORDER BY BNO DESC)
						)
		WHERE (RNUM >= 1 AND RNUM <= 10)
        ORDER BY BNO DESC, REGDATE DESC ;   		

	기능 순서        
1. 테이블 확인 -> VO, DTO
2. DAO -> 기능 추가(method)
3. mapper 추가
4. DAOImpl method 구현
5. read -> 조회수  ServiceImpl readmethod() 구현        		